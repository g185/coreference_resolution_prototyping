datamodule:
  _target_: data.pl_data_modules.BasePLDataModule
  datasets:
    train:
      - _target_: data.datasets.OntonotesDataset
        name: 'train'
        path:
          - 'data/prepare_ontonotes/train.english.jsonlines'
        max_doc_len: 2048
        processed_dataset_path: 'data/cache_${data.datamodule.datasets.train[0].max_doc_len}/train'
        tokenizer: "${model.module.model.huggingface_model_name}"
        mode: "${model.module.model.mode}"
        mask: True
    val:
      - _target_: data.datasets.OntonotesDataset
        name: 'val'
        path:
          - 'data/prepare_ontonotes/dev.english.jsonlines'
        max_doc_len: 2048
        processed_dataset_path: 'data/cache_${data.datamodule.datasets.val[0].max_doc_len}/val'
        tokenizer: "${model.module.model.huggingface_model_name}"
        mode: "${model.module.model.mode}"
    test:
      - _target_: data.datasets.OntonotesDataset
        name: 'test'
        path:
          - 'data/prepare_ontonotes/test.english.jsonlines'
        max_doc_len: 2048
        processed_dataset_path: 'data/cache_${data.datamodule.test[0].max_doc_len}/test'
        tokenizer: "${model.module.model.huggingface_model_name}"
        mode: "${model.module.model.mode}"



  batch_sizes:
    train: 1
    val: 1
    test: 1

  num_workers:
    train: 0
    val: 0
    test: 0

